export type Dish = {
    readonly id: string
    name: string
    status: string
}

export type Restaurant = {
    readonly id: string
    name: string
    address: string
    website: string
    status: string
}
